{
  "name": "lekhra-writes",
  "version": "1.0.0",
  "description": "Lekhra Writes creative studio and portfolio",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "mkdir -p dist && cp -a public/. dist/",
    "lint": "node -c server.js"
  },
  "dependencies": {
    "cookie": "^0.6.0",
    "express": "^4.19.2"
  }
}
